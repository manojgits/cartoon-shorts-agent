name: Run Cartoon Agent

on:
  schedule:
    # Run twice daily at 9:00 AM IST (3:30 AM UTC) and 6:00 PM IST (12:30 PM UTC)
    - cron: '30 3,12 * * *'
  workflow_dispatch:  # Allow manual trigger from GitHub UI

permissions:
  contents: write  # Needed to commit posted_videos.json back

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 🔑 Create Google Drive token file
        run: |
          if [ "${{ secrets.TOKEN }}" != "" ]; then
            echo '${{ secrets.TOKEN }}' > token.json
          fi

      - name: 🤖 Run Cartoon Agent
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: 💾 Commit updated posted_videos.json
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          if [ -f posted_videos.json ]; then git add posted_videos.json; fi
          git diff --cached --quiet || git commit -m "🤖 Update posted videos tracker"
          git push

      - name: 🧹 Cleanup
        if: always()
        run: rm -f token.json && rm -rf downloads/
